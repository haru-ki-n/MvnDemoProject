hello! git command

// add
git add ファイル名
git add -A

// 確認
git status

// commit
git commit -m "コメント"

// 確認
git log

// push
git push -u アップロード先 ブランチ名

// repository設定
git remote add origin https://github.com/haru-ki-n/MvnDemoProject.git

// ブランチを変更
git branch -M main
git push -u origin main